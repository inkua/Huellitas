export const metadata = {
    title: 'Administrador',
    description: 'Pagina para los administradores del sitio web.',
};

function layout({ children }) {
    return <div>{children}</div>;
}

export default layout;
